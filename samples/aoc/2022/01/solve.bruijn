:import std/Combinator .
:import std/Math .
:import std/String .

main blocks → (map (map string→number)) → (map sum) → sort-desc → res
	res [^0 : (^0 + ^(~0) + ^(~(~0)))]
	blocks [split-list-by (eq? "\n") (lines 0)]
